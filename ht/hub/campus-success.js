/** Student success: early alerts and advisor caseloads (placeholder until built). */
export function renderSuccess(view, ctx) { return `<section class="campus-panel"><h2>Student success</h2><p class="campus-muted">Coming together now.</p></section>`; }
export function bindSuccess() { return () => {}; }
